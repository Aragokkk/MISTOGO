.navigation-arrow {
  width: 70px; 
  height: 70px; 
  background: var(--white-nuche); 
  border: none; 
  border-radius: 17px; 
  cursor: pointer;
  display: flex; 
  align-items: center; 
  justify-content: center; 
  transition: all 0.3s ease; 
  position: absolute; 
  right: 0;
}
.navigation-arrow:hover { 
  background: #FFFFFF; 
  transform: translateX(5px); 
}
.navigation-arrow svg { 
  width: 38px; 
  height: 36px; 
}
.navigation-arrow svg path { 
  stroke: var(--green); 
  stroke-width: 3px; 
}/* Home.css - Повний адаптивний код */

.home-page,
.home-page *,
.home-page *::before,
.home-page *::after {
  box-sizing: border-box;
}

.home-page {
  width: 100%;
  margin: 0 auto;
  background: #ffffff;
  overflow-x: hidden;
}

/* ================= HERO ================= */

.hero-section {
  width: 100%;
  min-height: 100vh;
  margin: 0 auto;
  position: relative;
  background: linear-gradient(180deg, #8DBA97 0%, #7CACAB 100%);
  overflow: hidden;
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  align-items: center;
  box-sizing: border-box;
  padding: clamp(40px, 8vh, 80px) 5%;
  gap: clamp(60px, 8vw, 120px);
}

.hero-content {
  position: relative;
  z-index: 20;
  width: 100%;
  max-width: 1165px;
  pointer-events: none;
  transform: translateY(-50px);
}
.hero-content * { pointer-events: auto; }

.hero-title {
  font-family: var(--font-family);
  font-weight: 700;
  font-size: 74px;
  line-height: 110%;
  color: var(--white-text);
  margin: 0 0 30px 0;
  width: 1165px;
  height: 212px;
}

.hero-description {
  font-family: var(--font-family);
  font-size: 24px;
  font-weight: 500;
  line-height: 150%;
  color: var(--white-nuche);
  margin: 0 0 50px 0;
  opacity: 1;
  width: 767px;
  height: 58px;
}

.hero-button {
  width: 320px;
  height: 70px;
  background: transparent;
  border: 2px solid var(--white-text);
  border-radius: 35px;
  font-family: var(--font-family);
  font-weight: 600;
  font-size: 18px;
  color: var(--white-text);
  cursor: pointer;
  transition: all 0.3s ease;
}
.hero-button:hover {
  background: var(--white-text);
  color: #8DBA97;
  transform: translateY(-2px);
}

.hero-image {
  position: relative;
  width: 100%;
  aspect-ratio: 16/9;
  max-height: 1080px;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: visible;
}

.hero-image img {
  width: auto;
  height: 100%;
  min-width: 1920px;
  min-height: 1080px;
  object-fit: contain;
  opacity: 1;
  filter: drop-shadow(0 15px 25px rgba(0, 0, 0, 0.15));
  transform: translate(-70%, -10%);
}

.hero-footer {
  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  font-family: var(--font-family);
  font-size: clamp(14px, 1.8vw, 18px);
  color: var(--white-text);
  opacity: 0.9;
  z-index: 2;
}

/* ================= Transport + About ================= */

.transport-about-section {
  width: 100%;
  min-height: auto;
  margin: 0 auto;
  padding: clamp(40px, 6vh, 80px) 5%;
  background: linear-gradient(180deg, #7CACAB 0%, #8DBA97 100%);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: clamp(40px, 6vh, 80px);
}

.section-content {
  width: 100%;
}

.section-title {
  font-family: var(--font-family);
  font-weight: 700;
  font-size: clamp(32px, 4vw, 48px);
  max-width: 100%;
  text-align: center;
  color: var(--white-text);
  margin: 0 auto clamp(30px, 4vh, 40px);
  display: flex;
  align-items: center;
  justify-content: center;
}

.transport-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: clamp(24px, 3vw, 40px);
  max-width: 100%;
  margin: 0 auto;
  width: 100%;
}

.transport-card {
  width: 100%;
  min-height: 483px;
  background: var(--silver);
  border-radius: 36px;
  box-shadow: 0 0 12px 0 rgba(0, 0, 0, 0.25);
  padding: clamp(30px, 4vw, 40px) clamp(20px, 3vw, 30px) clamp(20px, 3vw, 30px);
  display: flex;
  flex-direction: column;
  align-items: center;
  box-sizing: border-box;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.transport-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
}

.transport-image {
  width: 100%;
  height: clamp(200px, 30vh, 280px);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: clamp(15px, 2vh, 20px);
  flex-shrink: 0;
}
.transport-image img {
  width: 120%;
  height: auto;
  max-width: 120%;
  object-fit: contain;
}

.transport-name {
  font-family: var(--font-family);
  font-weight: 600;
  font-size: clamp(20px, 2.5vw, 24px);
  color: #2C3E50;
  margin: 0 0 auto 0;
  text-align: center;
  flex-grow: 1;
  display: flex;
  align-items: center;
}

.transport-button {
  width: 100%;
  height: 50px;
  background: #A4C96B;
  border: none;
  border-radius: 25px;
  font-family: var(--font-family);
  font-weight: 600;
  font-size: 18px;
  color: var(--white-text);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  transition: all 0.3s ease;
  flex-shrink: 0;
}
.transport-button:hover { background: #91B556; transform: translateX(5px); }
.button-arrow { font-size: 24px; transition: transform 0.3s ease; }
.transport-button:hover .button-arrow { transform: translateX(5px); }

.about-content-wrapper {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: auto;
}
.about-title { margin-bottom: 30px; }
.about-content { 
  width: min(864px, 100%);
  margin: 0 auto;
}
.about-text {
  font-family: var(--font-family);
  font-size: clamp(16px, 2vw, 20px);
  font-weight: 400;
  line-height: 160%;
  color: var(--white-text);
  text-align: center;
  margin: 0 0 clamp(20px, 3vh, 24px) 0;
  opacity: 0.95;
}
.about-text:last-child { margin-bottom: 0; }

/* ================= Advantages ================= */

.advantages-section {
  width: 100%;
  min-height: auto;
  margin: 0 auto;
  padding: clamp(40px, 6vh, 80px) 5%;
  background: var(--white-nuche);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: clamp(40px, 5vh, 60px);
}

.advantages-title {
  max-width: 100%;
  color: #2C3E50;
  margin: 0 auto clamp(30px, 4vh, 50px);
}

.advantages-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: clamp(24px, 3vw, 40px);
  max-width: 100%;
  margin: 0 auto clamp(40px, 5vh, 50px) auto;
  width: 100%;
}

.advantage-card {
  width: 100%;
  min-height: 219px;
  background: var(--silver);
  border-radius: 30px;
  padding: clamp(20px, 3vw, 30px);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.advantage-card:nth-child(1) {
  grid-column: span 2;
}

.advantage-card:nth-child(2) {
  grid-column: span 1;
}

.advantage-card:nth-child(3) {
  grid-column: span 1;
}

.advantage-card:nth-child(4),
.advantage-card:nth-child(5),
.advantage-card:nth-child(6),
.advantage-card:nth-child(7) {
  grid-column: span 1;
}

.advantage-card:hover { 
  transform: translateY(-5px); 
  box-shadow: 0 10px 30px rgba(0,0,0,0.1); 
}

.advantage-icon { margin-bottom: 15px; }
.advantage-icon svg { width: clamp(40px, 5vw, 54px); height: clamp(40px, 5vw, 54px); }

.advantage-title {
  font-family: var(--font-family);
  font-weight: 600;
  font-size: clamp(18px, 2vw, 20px);
  color: #2C3E50;
  margin: 0 0 10px 0;
}
.advantage-description {
  font-family: var(--font-family);
  font-size: clamp(14px, 1.6vw, 16px);
  font-weight: 400;
  line-height: 140%;
  color: #2C3E50;
  margin: 0;
  opacity: 0.8;
}

/* Promo Banner */
.promo-banner {
  width: 100%;
  max-width: 100%;
  height: auto;
  min-height: 246px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr;
  border-radius: 30px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(0,0,0,0.1);
}

@media (min-width: 769px) {
  .promo-banner {
    grid-template-columns: 1fr 1fr;
  }
}

.promo-image { 
  width: 100%; 
  height: 100%;
  min-height: 246px;
  overflow: hidden; 
}
.promo-image img { 
  width: 100%; 
  height: 100%; 
  object-fit: cover;
  object-position: center;
}

.promo-content {
  width: 100%; 
  min-height: 246px;
  background: linear-gradient(142deg, #8dba97 0%, #7cacab 100%);
  border-radius: 0;
  padding: clamp(30px, 4vw, 50px);
  box-sizing: border-box;
  display: flex; 
  flex-direction: column; 
  justify-content: space-between;
  position: relative;
}

@media (min-width: 769px) {
  .promo-content {
    border-radius: 0 30px 30px 0;
  }
}

.promo-title {
  font-family: var(--font-family);
  font-weight: 700;
  font-size: clamp(18px, 2.5vw, 24px);
  line-height: 130%;
  color: var(--white-text);
  margin: 0;
}

.promo-subtitle {
  font-family: var(--font-family);
  font-size: clamp(16px, 2vw, 18px);
  font-weight: 400;
  color: var(--white-text);
  margin: 10px 0 0 0;
  opacity: 0.9;
}

.promo-button {
  width: 288px; 
  height: 59px;
  background: var(--white-text);
  border: none; 
  border-radius: 30px;
  padding: 10px 20px;
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 18px;
  color: #4B4B4B; 
  cursor: pointer;
  display: flex; 
  align-items: center; 
  justify-content: center; 
  gap: 8px;
  transition: all 0.3s ease;
  align-self: flex-end; 
  margin-top: auto; 
  margin-right: 50px; 
  box-sizing: border-box;
}
.promo-button:hover { 
  transform: translateX(5px); 
  box-shadow: 0 5px 15px rgba(0,0,0,0.2); 
  background: #FFFFFF; 
}
.promo-button .button-arrow { 
  width: 33px; 
  height: 33px; 
  transition: transform 0.3s ease; 
}
.promo-button:hover .button-arrow { 
  transform: translateX(5px); 
}

/* ================= Popular Choices ================= */

.popular-section {
  width: 100%;
  min-height: auto;
  margin: 0 auto;
  padding: clamp(40px, 6vh, 80px) 5%;
  background: var(--white-nuche);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.popular-title {
  max-width: 100%;
  font-family: var(--font-family);
  font-weight: 700;
  font-size: clamp(28px, 3.5vw, 36px);
  text-align: center;
  color: var(--gray-text);
  margin: 0 auto clamp(60px, 10vh, 140px);
  display: flex;
  align-items: center;
  justify-content: center;
}

.popular-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: clamp(30px, 4vw, 40px);
  max-width: 100%;
  margin: 0 auto;
  width: 100%;
}

.popular-card {
  width: 100%;
  min-height: 663px;
  background: var(--silver);
  border-radius: 30px;
  padding: 0;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  position: relative;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  overflow: visible;
}
.popular-card:hover { 
  transform: translateY(-5px); 
  box-shadow: 0 15px 40px rgba(0,0,0,0.15); 
}

.card-badge {
  position: absolute;
  top: clamp(-60px, -8vh, -80px);
  left: 50%;
  transform: translateX(-50%);
  padding: clamp(12px, 2vh, 17px) clamp(16px, 2vw, 24px);
  width: min(257px, 90%);
  height: auto;
  min-height: 89px;
  background: #CACACA; 
  border-radius: 20px;
  display: flex; 
  align-items: center; 
  gap: 12px;
  box-sizing: border-box; 
  z-index: 2;
}
.card-badge svg { 
  width: clamp(30px, 4vw, 39px); 
  height: clamp(30px, 4vw, 39px); 
  flex-shrink: 0; 
}
.card-badge span {
  font-family: var(--font-family); 
  font-weight: 500; 
  font-size: clamp(12px, 1.4vw, 14px); 
  line-height: 130%;
  color: var(--gray-text);
}

.card-image {
  width: 100%; 
  height: clamp(200px, 30vh, 280px);
  display: flex; 
  align-items: center; 
  justify-content: center;
  margin-bottom: 20px; 
  border-radius: 30px 30px 0 0;
  overflow: hidden; 
  background: #1a1a1a; 
  margin-top: 0;
}
.card-image img { 
  width: 100%; 
  height: 100%; 
  object-fit: contain; 
}

.card-name {
  font-family: var(--font-family); 
  font-weight: 700; 
  font-size: clamp(20px, 2.5vw, 24px);
  text-align: center; 
  color: var(--color-text); 
  margin: 0 0 20px 0;
}

.card-specs { 
  display: flex; 
  justify-content: center; 
  gap: clamp(20px, 3vw, 30px); 
  margin-bottom: 20px; 
  padding: 0 20px; 
  flex-wrap: wrap;
}
.spec-item { 
  display: flex; 
  align-items: center; 
  gap: 8px; 
}
.spec-item svg { 
  width: clamp(30px, 4vw, 39px); 
  height: clamp(30px, 4vw, 39px); 
  flex-shrink: 0; 
}
.spec-item span { 
  font-family: var(--font-family); 
  font-weight: 500; 
  font-size: clamp(14px, 1.6vw, 16px); 
  color: var(--gray-text); 
}

.card-footer { 
  display: flex; 
  justify-content: space-between; 
  align-items: center; 
  margin-bottom: 15px; 
  padding: 0 clamp(20px, 3vw, 30px); 
  flex-wrap: wrap;
  gap: 15px;
}
.card-price { 
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: clamp(18px, 2vw, 20px); 
  color: var(--gray-text); 
}

.card-button-white {
  width: 146px; 
  height: 53px; 
  background: #cacaca; 
  border: none; 
  border-radius: 30px;
  padding: 15px 20px; 
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 16px; 
  color: var(--gray-text);
  cursor: pointer; 
  transition: all 0.3s ease; 
  box-sizing: border-box;
}
.card-button-white:hover { 
  background: #b8b8b8; 
  transform: translateY(-2px); 
}

.card-button-green {
  width: 336px; 
  height: 59px; 
  background: var(--green); 
  border: none; 
  border-radius: 30px;
  padding: 10px 20px; 
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 18px; 
  color: #1D3A17;
  cursor: pointer; 
  transition: all 0.3s ease; 
  box-sizing: border-box; 
  margin: 0 auto 20px;
}
.card-button-green:hover { 
  background: #91B556; 
  transform: translateY(-2px); 
}

/* ================= Reviews ================= */

.reviews-section {
  width: 100%;
  min-height: auto;
  margin: 0 auto;
  padding: clamp(40px, 6vh, 80px) 5%;
  background: linear-gradient(180deg, #8DBA97 0%, #7CACAB 100%);
  box-sizing: border-box;
  display: flex; 
  flex-direction: column; 
  align-items: center;
  gap: clamp(30px, 4vh, 40px);
}

.reviews-title {
  max-width: 100%;
  font-family: var(--font-family); 
  font-weight: 700; 
  font-size: clamp(28px, 3.5vw, 36px); 
  text-align: center; 
  color: var(--white-nuche);
  margin: 0 auto clamp(15px, 2vh, 20px); 
  display: flex; 
  align-items: center; 
  justify-content: center;
}

.reviews-subtitle {
  max-width: min(790px, 100%);
  font-family: var(--second-family); 
  font-weight: 500; 
  font-size: clamp(14px, 1.6vw, 16px); 
  text-align: center; 
  color: var(--white-nuche);
  margin: 0 auto clamp(30px, 4vh, 50px); 
  opacity: 0.95;
}

.reviews-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
  gap: clamp(24px, 3vw, 40px); 
  justify-content: center; 
  margin-bottom: clamp(30px, 4vh, 40px); 
  width: 100%;
  max-width: 100%;
}

.review-card {
  width: 100%;
  max-width: none;
  min-height: 254px;
  background: #F5F5F5; 
  border-radius: 30px;
  padding: 0; 
  box-sizing: border-box; 
  display: flex; 
  flex-direction: column; 
  position: relative;
  transition: transform 0.3s ease, box-shadow 0.3s ease; 
  overflow: hidden;
}
.review-card:hover { 
  transform: translateY(-5px); 
  box-shadow: 0 15px 40px rgba(0,0,0,0.15); 
}

.review-header {
  display: flex; 
  align-items: center; 
  justify-content: space-between; 
  margin-bottom: 20px;
  position: relative; 
  padding: clamp(20px, 3vw, 30px) clamp(20px, 3vw, 30px) 0 clamp(20px, 3vw, 30px);
  flex-wrap: wrap;
  gap: 15px;
}
.reviewer-info { 
  display: flex; 
  align-items: center; 
  gap: 15px; 
}

.reviewer-avatar { 
  width: clamp(35px, 5vw, 44px); 
  height: clamp(35px, 5vw, 43px); 
  border-radius: 50%; 
  overflow: hidden; 
  flex-shrink: 0;
  filter: drop-shadow(0 4px 4px rgba(0,0,0,0.25)); 
}
.reviewer-avatar img { 
  width: 100%; 
  height: 100%; 
  object-fit: cover; 
}

.reviewer-name { 
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: clamp(16px, 1.8vw, 18px); 
  color: var(--gray-text); 
}

.review-vehicle {
  position: absolute; 
  top: 0; 
  right: 0;
  width: 307px; 
  height: 72px; 
  background: #D9D9D9; 
  border-radius: 0 30px 0 30px;
  display: flex; 
  align-items: center; 
  justify-content: center; 
  padding: 10px 20px; 
  box-sizing: border-box;
}
.review-vehicle span { 
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: clamp(14px, 1.6vw, 16px); 
  text-align: center; 
  color: var(--gray-text); 
}

.review-text {
  font-family: var(--font-family); 
  font-size: clamp(14px, 1.6vw, 16px); 
  font-weight: 400; 
  line-height: 150%; 
  color: var(--gray-text);
  margin: 0 0 20px 0; 
  flex-grow: 1; 
  padding: 0 clamp(20px, 3vw, 30px);
}

.review-footer { 
  display: flex; 
  align-items: center; 
  justify-content: space-between; 
  padding: 0 clamp(20px, 3vw, 30px) clamp(20px, 3vw, 30px) clamp(20px, 3vw, 30px); 
  flex-wrap: wrap;
  gap: 15px;
}

.review-rating { 
  display: flex; 
  gap: 8px; 
}
.star { 
  width: clamp(30px, 4vw, 40px); 
  height: clamp(30px, 4vw, 40px); 
  display: flex; 
  align-items: center; 
  justify-content: center; 
}
.star svg { 
  width: clamp(20px, 3vw, 26px); 
  height: clamp(20px, 3vw, 26px); 
}
.star.filled svg path { 
  fill: #F9E31B; 
}
.star.empty svg path { 
  fill: #CACACA; 
}

.review-date { 
  font-family: var(--font-family); 
  font-size: clamp(12px, 1.4vw, 14px); 
  font-weight: 400; 
  color: var(--gray-text); 
  opacity: 0.7; 
}

.reviews-actions { 
  display: flex; 
  align-items: center; 
  justify-content: center; 
  margin-top: auto; 
  width: 100%; 
  position: relative; 
  gap: 20px;
  flex-wrap: wrap;
}

.add-review-button {
  width: 288px; 
  height: 59px;
  background: transparent; 
  border: 2px solid var(--white-nuche); 
  border-radius: 30px;
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 16px; 
  color: var(--white-nuche);
  cursor: pointer; 
  display: flex; 
  align-items: center; 
  justify-content: center; 
  gap: 10px; 
  transition: all 0.3s ease;
}
.add-review-button:hover { 
  background: var(--white-nuche); 
  color: #8DBA97; 
  transform: translateY(-2px); 
}
.add-review-button svg { 
  width: 24px; 
  height: 24px; 
}
.add-review-button svg path { 
  stroke: var(--white-nuche); 
  stroke-width: 3px; 
  transition: stroke 0.3s ease; 
}
.add-review-button:hover svg path { 
  stroke: #8DBA97; 
}

.navigation-arrow {
  width: clamp(60px, 8vw, 70px); 
  height: clamp(60px, 8vw, 70px); 
  background: var(--white-nuche); 
  border: none; 
  border-radius: 17px; 
  cursor: pointer;
  display: flex; 
  align-items: center; 
  justify-content: center; 
  transition: all 0.3s ease;
}
.navigation-arrow:hover { 
  background: #FFFFFF; 
  transform: translateX(5px); 
}
.navigation-arrow svg { 
  width: clamp(30px, 4vw, 38px); 
  height: clamp(28px, 3.5vw, 36px); 
}
.navigation-arrow svg path { 
  stroke: var(--green); 
  stroke-width: 3px; 
}

/* ================= Footer ================= */

.footer-section {
  width: 100%;
  min-height: auto;
  margin: 0 auto;
  background: #4B4B4B;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

.footer-top {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: clamp(20px, 3vh, 30px) 5%; 
  border-bottom: 1px solid rgba(255,255,255,0.2);
  gap: 20px;
  max-width: 100%;
  width: 100%;
  margin: 0 auto;
}

.footer-nav { 
  display: flex; 
  gap: clamp(20px, 3vw, 40px); 
  align-items: center; 
  flex-wrap: wrap;
  justify-self: start;
}
.footer-nav a {
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: clamp(14px, 1.6vw, 16px);
  color: var(--white-text); 
  text-decoration: none; 
  transition: color 0.3s ease;
}
.footer-nav a:hover { 
  color: var(--green); 
}

.footer-center { 
  display: flex; 
  flex-direction: column; 
  align-items: center; 
  gap: 10px;
  justify-self: center;
}
.footer-logo {
  font-family: var(--font-family); 
  font-weight: 700; 
  font-size: clamp(20px, 2.5vw, 24px); 
  font-style: italic; 
  color: var(--white-text); 
  margin: 0;
}
.footer-tagline {
  font-family: var(--second-family); 
  font-weight: 400; 
  font-size: clamp(11px, 1.2vw, 12px); 
  text-align: center;
  color: var(--white-text); 
  opacity: 0.8; 
  margin: 0; 
  max-width: min(300px, 100%);
}

.footer-right { 
  display: flex; 
  align-items: center; 
  gap: 16px; 
  flex-wrap: wrap;
  justify-self: end;
}
.footer-questions { 
  background: transparent;
  border: 0;
  cursor: pointer;
  color: var(--white-text);
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 16px;
  height: 44px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  transition: opacity 0.2s;
}
.footer-questions:hover {
  opacity: 0.85;
}
.footer-language { 
  background: transparent;
  border: 0;
  cursor: pointer;
  color: var(--white-text);
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 16px;
  height: 44px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  transition: opacity 0.2s;
}
.footer-language:hover {
  opacity: 0.85;
}

.footer-login {
  width: 90px; 
  height: 40px; 
  background: transparent; 
  border: 2px solid var(--white-text); 
  border-radius: 20px;
  font-family: var(--font-family); 
  font-weight: 600; 
  font-size: 14px; 
  color: var(--white-text);
  cursor: pointer; 
  transition: all 0.3s ease;
}
.footer-login:hover { 
  background: var(--white-text); 
  color: #4B4B4B; 
}

.footer-theme-toggle {
  width: 59px; 
  height: 31px; 
  background: transparent; 
  border: 0;
  padding: 0;
  display: flex; 
  align-items: center; 
  justify-content: center; 
  cursor: pointer; 
  transition: transform 0.1s ease; 
  overflow: visible;
}
.footer-theme-toggle:active { 
  transform: scale(0.96); 
}
.footer-theme-toggle svg { 
  display: block;
  width: 59px; 
  height: 31px; 
  overflow: visible; 
}

.footer-bottom { 
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center; 
  padding: clamp(15px, 2vh, 20px) 5%; 
  gap: 20px;
  max-width: 100%;
  width: 100%;
  margin: 0 auto;
}

.footer-legal { 
  display: flex; 
  gap: clamp(20px, 3vw, 30px); 
  flex-wrap: wrap;
  justify-self: start;
}
.footer-legal a {
  font-family: var(--font-family); 
  font-weight: 400; 
  font-size: clamp(12px, 1.4vw, 14px); 
  color: var(--white-text);
  text-decoration: none; 
  opacity: 0.8; 
  transition: opacity 0.3s ease;
}
.footer-legal a:hover { 
  opacity: 1; 
}

.footer-social { 
  display: flex; 
  gap: 20px; 
  align-items: center; 
  flex-wrap: wrap;
  justify-self: center;
}
.footer-email {
  font-family: var(--font-family); 
  font-weight: 400; 
  font-size: clamp(12px, 1.4vw, 14px); 
  color: var(--white-text);
  text-decoration: none; 
  opacity: 0.8; 
  transition: opacity 0.3s ease;
}
.footer-email:hover { 
  opacity: 1; 
}

.social-icons { 
  display: flex; 
  gap: 15px; 
}
.social-icon {
  width: clamp(20px, 2.5vw, 24px); 
  height: clamp(20px, 2.5vw, 24px); 
  display: flex; 
  align-items: center; 
  justify-content: center; 
  cursor: pointer; 
  transition: transform 0.3s ease;
}
.social-icon:hover { 
  transform: translateY(-3px); 
}
.social-icon svg { 
  width: clamp(20px, 2.5vw, 24px); 
  height: clamp(20px, 2.5vw, 24px); 
  fill: var(--white-text); 
}

.footer-credits { 
  display: flex; 
  gap: clamp(20px, 3vw, 30px); 
  align-items: center; 
  flex-wrap: wrap;
  justify-self: end;
}
.footer-cookies, .footer-copyright {
  font-family: var(--font-family); 
  font-weight: 400; 
  font-size: clamp(12px, 1.4vw, 14px); 
  color: var(--white-text); 
  opacity: 0.8;
}

/* ================= RESPONSIVE ================= */

@media (max-width: 1440px) {
  .transport-cards {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .advantages-grid {
    grid-template-columns: repeat(4, 1fr);
  }
  
  .advantage-card:nth-child(1) {
    grid-column: span 2;
  }
  
  .advantage-card:nth-child(2) {
    grid-column: span 1;
  }
  
  .advantage-card:nth-child(3) {
    grid-column: span 1;
  }
  
  .advantage-card:nth-child(4),
  .advantage-card:nth-child(5),
  .advantage-card:nth-child(6),
  .advantage-card:nth-child(7) {
    grid-column: span 1;
  }
  
  .popular-cards {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 1200px) {
  .advantages-grid {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .advantage-card:nth-child(1),
  .advantage-card:nth-child(2),
  .advantage-card:nth-child(3) {
    grid-column: span 1;
  }
  
  .advantage-card:nth-child(4),
  .advantage-card:nth-child(5),
  .advantage-card:nth-child(6),
  .advantage-card:nth-child(7) {
    grid-column: span 1;
  }
}

@media (max-width: 1024px) {
  .hero-section {
    grid-template-columns: 1fr;
    padding-top: 80px;
    padding-bottom: 40px;
    gap: 30px;
  }
  
  .hero-content {
    max-width: 100%;
    text-align: center;
  }
  
  .hero-image {
    width: 100%;
    height: 40vh;
  }
  
  .transport-cards {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .popular-cards {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .reviews-container {
    grid-template-columns: 1fr;
  }
  
  .footer-top {
    grid-template-columns: 1fr;
    text-align: center;
  }
  
  .footer-nav,
  .footer-center,
  .footer-right {
    justify-self: center;
  }
  
  .footer-bottom {
    grid-template-columns: 1fr;
  }
  
  .footer-legal,
  .footer-social,
  .footer-credits {
    justify-self: center;
  }
}

@media (max-width: 768px) {
  .hero-section {
    min-height: 90vh;
    padding-top: 60px;
  }
  
  .hero-image {
    height: 35vh;
  }
  
  .transport-cards {
    grid-template-columns: 1fr;
  }
  
  .advantages-grid {
    grid-template-columns: 1fr;
  }
  
  .advantage-card:nth-child(1),
  .advantage-card:nth-child(2),
  .advantage-card:nth-child(3),
  .advantage-card:nth-child(4),
  .advantage-card:nth-child(5),
  .advantage-card:nth-child(6),
  .advantage-card:nth-child(7) {
    grid-column: span 1;
  }
  
  .popular-cards {
    grid-template-columns: 1fr;
  }
  
  .reviews-container {
    grid-template-columns: 1fr;
  }
  
  .footer-nav {
    flex-direction: column;
    gap: 15px;
  }
  
  .footer-legal,
  .footer-credits {
    flex-direction: column;
    gap: 10px;
  }
}